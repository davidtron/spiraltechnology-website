
extends layout

block append vars
  - bodyclass = 'article-detail'

block prepend title
  | #{ page.title + " - "}

block content
  article.article
    h1= page.title
    section.content!= typogr(page.html).typogrify()

  div.nav
    a(href=contents.index.url) « Back to blog

append footer
  +background('/img/woven.jpg')

